AWS_REGION=us-east-1
MODEL_ID=us.amazon.nova-pro-v1:0

# Used by ui/app.py to invoke the deployed agent.
# Get this from `agentcore status` or the deploy output.
AGENTCORE_RUNTIME_ARN=arn:aws:bedrock-agentcore:....