.venv/
__pycache__/
*.pyc
.env
.idea/
.vscode/

# AgentCore toolkit regenerates these on `agentcore configure`/`deploy`
.bedrock_agentcore/

# Holds environment-specific values (account ID, runtime ARN, ECR URI, role
# names) post-deploy. Regenerated by the toolkit, so safe to gitignore.
.bedrock_agentcore.yaml

# Scratch artifacts from runtime smoke testing
output.json
payload.json
